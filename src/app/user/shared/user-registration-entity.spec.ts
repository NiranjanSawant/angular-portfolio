import { UserRegistrationEntity } from './user-registration-entity';

describe('UserRegistrationEntity', () => {
  it('should create an instance', () => {
    expect(new UserRegistrationEntity()).toBeTruthy();
  });
});
